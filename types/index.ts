/** Prisma provides typings directly from the models you created using the `prisma generate` command */
export type { Post, Prisma } from "@prisma/client";

/**
 * Additionally it provides typings for all the operations that can be done on your Post model e.g.:
 * PostCreateArgs, PostDeleteArgs etc.
 *
 * These are exported from the Prisma namespace.
 */
